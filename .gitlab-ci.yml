stages:
  - build

variables:
  SYSTEM: "lucas"

nixos-build:
  stage: build
  tags:
    - nix
  only:
    - nixos
  script:
    - git rm flake.lock
    - nix flake update
    - nix build .#nixosConfigurations.${SYSTEM}.config.system.build.toplevel -j 1;
    - attic push campground ./result

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: netbird
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"  # After Prometheus and external-secrets
spec:
  project: platform

  sources:
    # Helm chart
    - repoURL: https://netbirdio.github.io/helms
      chart: netbird
      targetRevision: 0.1.0  # Latest stable
      helm:
        valueFiles:
          - $values/packages/kubernetes-gitops/apps/netbird/values.yaml

    # Values ref
    - repoURL: https://gitlab.com/usmcamp0811/dotfiles.git
      targetRevision: nixos
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: netbird

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

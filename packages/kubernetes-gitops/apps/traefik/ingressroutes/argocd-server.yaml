apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-self-server
  namespace: argocd
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`argocd.k8s.aicampground.com`)
      priority: 10
      services:
        - name: argocd-self-server
          port: 80
  tls:
    certResolver: cloudflare

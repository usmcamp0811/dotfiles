[tool.poetry]
name = "random_python_project"
version = "0.1.0"
description = "A Demo Python Project to show what its like using Cookiecutters everywhere."
authors = ["Matt Camp <matt@aicampground.com>"]

[tool.poetry.dependencies]
python = "^3.11"
maya = "^0.6.1"
regex = "^2024.7.24"

[tool.poetry.dev-dependencies]
pytest = "^8.1.1"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
